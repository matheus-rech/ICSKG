---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - config.yaml
  - .python-version
autonomous: true
requirements:
  - FOUN-01
  - FOUN-05

must_haves:
  truths:
    - "uv run python -c 'import pandas; import duckdb; import linearmodels' succeeds on a clean checkout"
    - "uv.lock file exists and commits all dependency hashes for reproducibility"
    - "All downstream scripts can locate data directories from config.yaml; pipeline does not hard-code paths"
    - "config.yaml defaults to data_sources/ so the project works without a NAS mount"
  artifacts:
    - path: "pyproject.toml"
      provides: "Pinned dependency declarations for all 16 project libraries"
      contains: "pandas>=2.2.3,<3.0"
    - path: "uv.lock"
      provides: "Reproducible lock file with exact hashes"
      contains: "[package]"
    - path: ".python-version"
      provides: "uv runtime pin to Python 3.12"
      contains: "3.12"
    - path: "config.yaml"
      provides: "NAS and local data path configuration"
      contains: "data_dir:"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv sync"
      pattern: "requires-python.*3\\.12"
    - from: "config.yaml"
      to: "database/pipeline.py"
      via: "loaded by pipeline at startup"
      pattern: "data_dir:"
---

<objective>
Bootstrap the reproducible Python 3.12 environment and project-wide configuration that every downstream script depends on.

Purpose: Without a pinned uv environment, the pipeline is not reproducible — re-running on a clean machine or a different OS will install different library versions and produce silently different results. Without a config file, NAS path handling requires code changes rather than a config edit.

Output: pyproject.toml with all 16 pinned dependencies, a committed uv.lock, a .python-version file selecting Python 3.12, and a config.yaml providing default data paths (NAS-aware, falling back to data_sources/).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md

<interfaces>
<!-- Key decisions from 01-CONTEXT.md that constrain implementation -->
<!-- D-06: All downloaded/processed data saved to NAS (not local) due to storage constraints -->
<!-- D-07: Config file for all data paths — NAS path, raw data directory, processed data directory. Default to data_sources/ if config not present -->
<!-- Decision from STATE.md: Python must upgrade from 3.9 → 3.12 (pysus 1.x and pandas 2.2.3 do not support 3.9) -->
<!-- Decision from STATE.md: uv + uv.lock required for reproducibility standard -->

<!-- Confirmed from STACK.md — exact version pins for pyproject.toml: -->
<!--
[project]
name = "icskg-br"
requires-python = ">=3.12,<3.14"
dependencies = [
    "pandas>=2.2.3,<3.0",
    "numpy>=2.0",
    "pyarrow>=19.0",
    "pysus>=1.0.1",
    "datasus-fetcher>=0.4.1",
    "requests>=2.32",
    "sidrapy>=0.1.4",
    "ipeadatapy>=0.1.9",
    "duckdb>=1.5.1",
    "scikit-learn>=1.6,<2.0",
    "xgboost>=2.1,<4.0",
    "linearmodels>=7.0",
    "matplotlib>=3.9",
    "seaborn>=0.13",
    "SciencePlots>=2.1",
    "geopandas>=1.1",
    "openpyxl>=3.1",
    "tqdm>=4.66",
    "python-dotenv>=1.0",
]
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create pyproject.toml and pin Python 3.12</name>
  <read_first>
    - CLAUDE.md (project constraints section — stack requirements)
    - .planning/research/STACK.md (exact version strings for all 16 dependencies)
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-06, D-07)
  </read_first>
  <files>pyproject.toml, .python-version</files>
  <action>
Create pyproject.toml at the project root with the following exact content:

```toml
[project]
name = "icskg-br"
version = "1.0.0"
description = "Municipal Urban Development and Global Surgery Panel Study — Brazil"
requires-python = ">=3.12,<3.14"
dependencies = [
    # Core data processing
    "pandas>=2.2.3,<3.0",
    "numpy>=2.0",
    "pyarrow>=19.0",
    # DATASUS / health data acquisition
    "pysus>=1.0.1",
    "datasus-fetcher>=0.4.1",
    "requests>=2.32",
    # Brazilian government APIs
    "sidrapy>=0.1.4",
    "ipeadatapy>=0.1.9",
    # Database layer
    "duckdb>=1.5.1",
    # Machine learning
    "scikit-learn>=1.6,<2.0",
    "xgboost>=2.1,<4.0",
    "linearmodels>=7.0",
    # Visualization
    "matplotlib>=3.9",
    "seaborn>=0.13",
    "SciencePlots>=2.1",
    # Geospatial
    "geopandas>=1.1",
    # Supporting utilities
    "openpyxl>=3.1",
    "tqdm>=4.66",
    "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```

Then create .python-version at project root containing exactly:
```
3.12
```

This pins uv to use Python 3.12. The pandas>=2.2.3,<3.0 constraint is CRITICAL — pandas 3.0 (Jan 2026) makes Copy-on-Write mandatory and changes string dtypes, which would break the existing codebase silently (per STACK.md research).

After creating both files, run:
```bash
uv sync
```

This generates uv.lock. If uv is not installed, run:
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```
then retry uv sync.
  </action>
  <verify>
    <automated>uv run python -c "import sys; assert sys.version_info[:2] == (3, 12), f'Expected 3.12, got {sys.version_info[:2]}'; import pandas; import duckdb; import linearmodels; print('Stack OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml exists at project root: `test -f pyproject.toml`
    - pyproject.toml contains requires-python with 3.12: `grep -q 'requires-python.*3\.12' pyproject.toml`
    - pyproject.toml contains pandas pin: `grep -q 'pandas>=2.2.3,<3.0' pyproject.toml`
    - pyproject.toml contains linearmodels pin: `grep -q 'linearmodels>=7.0' pyproject.toml`
    - pyproject.toml contains duckdb pin: `grep -q 'duckdb>=1.5.1' pyproject.toml`
    - .python-version exists and contains "3.12": `grep -q '3.12' .python-version`
    - uv.lock exists: `test -f uv.lock`
    - uv.lock is non-empty: `test -s uv.lock`
    - `uv run python --version` outputs "Python 3.12.x"
  </acceptance_criteria>
  <done>pyproject.toml pinning all 16 project dependencies committed, .python-version set to 3.12, uv.lock generated and committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create config.yaml for NAS-aware data path configuration</name>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (decisions D-06 and D-07 for NAS path and config approach; "specifics" section for actual NAS address)
  </read_first>
  <files>config.yaml, config.example.yaml</files>
  <action>
Create config.yaml at the project root (this is the actual config, NOT committed to git — it is machine-specific). Create config.example.yaml as the committed template.

config.example.yaml content:
```yaml
# ICSKG-BR Data Path Configuration
# Copy this file to config.yaml and adjust paths for your machine.
# If config.yaml is absent, pipeline.py defaults to data_sources/ (relative to project root).

# NAS or local data root. Set to your NAS mount path, e.g.:
#   smb://synology-rech._smb._tcp.local/docker/Downloads
# Or a local path such as /Volumes/NAS/Downloads or /mnt/nas/downloads
data_root: "data_sources"

# Subdirectories under data_root (relative paths from data_root)
raw_dir: "raw"
processed_dir: "processed"
quarantine_dir: "quarantine"

# Database output directory (relative to project root)
db_dir: "database"

# Output directory for analysis figures and tables
output_dir: "analysis/results"
```

config.yaml content (the local machine config for this project — NAS path from CONTEXT.md specifics section):
```yaml
# ICSKG-BR Data Path Configuration — LOCAL MACHINE CONFIG (do not commit)

# NAS mount path (Synology at smb://synology-rech._smb._tcp.local/docker/Downloads)
# After mounting: /Volumes/docker/Downloads  (macOS Finder mount point)
data_root: "/Volumes/docker/Downloads/ICSKG"

raw_dir: "raw"
processed_dir: "processed"
quarantine_dir: "quarantine"
db_dir: "database"
output_dir: "analysis/results"
```

Then update .gitignore to exclude config.yaml but not config.example.yaml. Check if .gitignore exists; if it does, append the following lines:
```
# Local machine configuration (contains NAS paths)
config.yaml
# Generated data and databases
data_sources/raw/
data_sources/processed/
data_sources/quarantine/
database/icskg_br.sqlite
database/icskg_br.duckdb
uv.lock
```

Note: uv.lock is intentionally tracked for reproducibility in research workflows — remove uv.lock from .gitignore if present.

Now update database/pipeline.py to load config.yaml at startup. Add a load_config() helper near the top of pipeline.py (after the logger setup, before the STAGES constant). This function must:
1. Look for config.yaml at project root (relative to the calling script's location, resolved via Path(__file__).parent.parent)
2. If absent, return default paths (data_sources/, database/)
3. Return a dict with keys: data_root, raw_dir, processed_dir, quarantine_dir, db_dir, output_dir

```python
def load_config(config_path: Path | None = None) -> dict:
    """Load data path configuration from config.yaml.

    Falls back to data_sources/ defaults if config.yaml is absent,
    ensuring the project works for any researcher without a NAS mount.
    """
    if config_path is None:
        config_path = Path(__file__).parent.parent / "config.yaml"
    if not config_path.exists():
        return {
            "data_root": "data_sources",
            "raw_dir": "raw",
            "processed_dir": "processed",
            "quarantine_dir": "quarantine",
            "db_dir": "database",
            "output_dir": "analysis/results",
        }
    try:
        import yaml  # noqa: PLC0415
        with config_path.open() as f:
            cfg = yaml.safe_load(f)
        return cfg
    except Exception as exc:  # noqa: BLE001
        logger.warning("Could not load config.yaml: %s — using defaults", exc)
        return {
            "data_root": "data_sources",
            "raw_dir": "raw",
            "processed_dir": "processed",
            "quarantine_dir": "quarantine",
            "db_dir": "database",
            "output_dir": "analysis/results",
        }
```

Add PyYAML to pyproject.toml dependencies: `"pyyaml>=6.0"`.
After adding PyYAML, run `uv sync` again to update uv.lock.
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; import yaml; cfg = yaml.safe_load(Path('config.example.yaml').read_text()); assert 'data_root' in cfg; assert 'raw_dir' in cfg; print('config.example.yaml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - config.example.yaml exists: `test -f config.example.yaml`
    - config.example.yaml contains data_root key: `grep -q 'data_root:' config.example.yaml`
    - config.example.yaml contains raw_dir key: `grep -q 'raw_dir:' config.example.yaml`
    - config.yaml exists: `test -f config.yaml`
    - config.yaml contains the NAS path: `grep -q 'ICSKG' config.yaml`
    - .gitignore excludes config.yaml: `grep -q 'config.yaml' .gitignore`
    - database/pipeline.py contains load_config function: `grep -q 'def load_config' database/pipeline.py`
    - pyproject.toml contains pyyaml: `grep -q 'pyyaml' pyproject.toml`
    - uv.lock regenerated (modification time is recent)
  </acceptance_criteria>
  <done>config.yaml (machine-local) and config.example.yaml (committed template) created; load_config() added to pipeline.py with fallback defaults; config.yaml excluded from git.</done>
</task>

</tasks>

<verification>
Full environment verification:
```bash
uv run python -c "
import sys
assert sys.version_info[:2] == (3, 12), f'Wrong Python: {sys.version_info}'
import pandas as pd
assert pd.__version__.startswith('2.'), f'pandas 3.x detected: {pd.__version__}'
import duckdb
assert duckdb.__version__ >= '1.5.1', f'DuckDB too old: {duckdb.__version__}'
import linearmodels
print(f'pandas {pd.__version__}, duckdb {duckdb.__version__}, linearmodels OK')
print('FOUN-01 / FOUN-05: PASS')
"
```
</verification>

<success_criteria>
- `uv run python -c "import pandas; import duckdb; import linearmodels"` exits 0
- Python version is 3.12.x (not 3.9 or 3.13)
- pandas version is 2.2.x (not 3.x — would break the existing codebase)
- uv.lock exists and is non-empty (reproducibility gate for journal submission)
- config.yaml provides NAS-aware path resolution; config.example.yaml is committed for other researchers
- load_config() in pipeline.py defaults gracefully when config.yaml is absent
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
