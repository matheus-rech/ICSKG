---
phase: 09-spatial-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analysis/spatial_analysis.py
  - tests/test_spatial_analysis.py
  - pyproject.toml
autonomous: true
requirements: [SPAT-01, SPAT-02, SPAT-03, SPAT-04]

must_haves:
  truths:
    - "Moran's I statistic and p-value on FE residuals exist in results/spatial/morans_i.csv with interpretation"
    - "CUDS choropleth renders all 5,570+ municipalities at 300 DPI with colorblind-safe diverging palette and state borders"
    - "Regional disparity CSV contains mean, median, IQR for CUDS and 6 LCoGS indicators across 5 macro-regions"
    - "LCoGS heatmaps by region show temporal trends (2015-2023) as publication-ready 300 DPI figures"
  artifacts:
    - path: "analysis/spatial_analysis.py"
      provides: "Spatial analysis module: Moran's I, choropleth, regional disparity, heatmaps"
      exports: ["compute_morans_i", "generate_cuds_choropleth", "compute_regional_disparity", "generate_lcogs_heatmaps", "main"]
    - path: "tests/test_spatial_analysis.py"
      provides: "Unit tests for spatial statistics and output structure"
      min_lines: 60
    - path: "results/spatial/morans_i.csv"
      provides: "Moran's I test results per LCoGS outcome"
    - path: "results/tables/regional_disparity.csv"
      provides: "Regional disparity summary statistics"
    - path: "results/figures/cuds_choropleth.png"
      provides: "CUDS choropleth map at 300 DPI"
    - path: "results/figures/lcogs_heatmap_*.png"
      provides: "LCoGS heatmap figures by region"
  key_links:
    - from: "analysis/spatial_analysis.py"
      to: "database/icskg_br.sqlite"
      via: "SQLite query for dimension_scores, lcogs_indicators, municipality_lookup"
      pattern: "sqlite3\\.connect|pd\\.read_sql"
    - from: "analysis/spatial_analysis.py"
      to: "analysis/run_regressions.py"
      via: "import load_regression_panel, run_panel_regressions for residuals"
      pattern: "from analysis\\.run_regressions import"
    - from: "analysis/spatial_analysis.py"
      to: "geobr"
      via: "read_municipality(year=2022) for shapefile"
      pattern: "geobr\\.read_municipality"
---

<objective>
Spatial autocorrelation testing, publication-ready choropleth maps, regional disparity analysis, and LCoGS heatmaps for all 5,570 Brazilian municipalities.

Purpose: Validate that regression residuals do not exhibit spatial clustering (Moran's I), and produce 4+ publication-ready figures documenting geographic CUDS and LCoGS distributions for BMJ Global Health submission.
Output: analysis/spatial_analysis.py module, results/spatial/morans_i.csv, results/tables/regional_disparity.csv, results/figures/ choropleth and heatmap PNGs at 300 DPI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-statistical-analysis/07-01-SUMMARY.md

@analysis/run_regressions.py
@analysis/compute_dimensions.py
@analysis/compute_lcogs.py
@analysis/ml_models.py
@database/build_database_v3.py
@extract_lcogs1.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From analysis/run_regressions.py:
```python
LCOGS_OUTCOMES = [
    ("lcogs1_distance_km",       "LCoGS-1: Bellwether Access (km)"),
    ("sao_per_100k",             "LCoGS-2: SAO Density per 100k"),
    ("surgical_volume_per_100k", "LCoGS-3: Surgical Volume per 100k"),
    ("pomr",                     "LCoGS-4: Perioperative Mortality"),
    ("financial_risk_ratio",     "LCoGS-5: Financial Risk Ratio"),
    ("catastrophic_expenditure", "LCoGS-6: Catastrophic Expenditure"),
]

def load_regression_panel(db_path: Path) -> pd.DataFrame:
    """Returns panel with MultiIndex (cod_ibge, year) containing cuds + 6 LCoGS columns."""

def run_panel_regressions(panel: pd.DataFrame) -> dict:
    """Returns dict mapping outcome_col -> PanelOLS result object.
    Result objects have .resids property (pd.Series with MultiIndex)."""
```

From database/build_database_v3.py:
```python
# municipality_lookup table schema:
#   cod_ibge TEXT PRIMARY KEY, name TEXT, state_uf TEXT, region TEXT
# region values: "Norte", "Nordeste", "Centro-Oeste", "Sudeste", "Sul"

UF_TO_REGION: dict[str, str] = {
    "AC": "Norte", ..., "SP": "Sudeste", ..., "SC": "Sul",
}
```

From analysis/compute_dimensions.py:
```python
DIM_COLS = ["D1", "D2", "D3", "D4", "D5", "D6", "D7", "D9"]
EPSILON = 0.01
```

SQLite tables available:
- dimension_scores: (cod_ibge, year, D1-D7, D9, cuds, n_dimensions)
- lcogs_indicators: (cod_ibge, year, lcogs1_distance_km, sao_per_100k, surgical_volume_per_100k, pomr, financial_risk_ratio, catastrophic_expenditure)
- municipality_lookup: (cod_ibge, name, state_uf, region)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spatial statistics -- Moran's I on FE residuals + regional disparity analysis</name>
  <files>analysis/spatial_analysis.py, tests/test_spatial_analysis.py, pyproject.toml</files>
  <read_first>
    @analysis/run_regressions.py (load_regression_panel, run_panel_regressions, LCOGS_OUTCOMES)
    @analysis/ml_models.py (load_ml_dataset for region group pattern)
    @database/build_database_v3.py (UF_TO_REGION, municipality_lookup schema)
    @database/impute_ifgf.py (UF_TO_REGION numeric mapping for reference)
    @pyproject.toml (current dependencies)
  </read_first>
  <behavior>
    - Test 1: compute_morans_i() returns DataFrame with columns [outcome, morans_i, p_value, z_score, n_obs, interpretation]
    - Test 2: Moran's I interpretation is "positive spatial autocorrelation" when I > 0 and p < 0.05, "no significant autocorrelation" otherwise
    - Test 3: compute_regional_disparity() returns DataFrame with columns [region, variable, mean, median, q25, q75, iqr, n] for all 5 regions
    - Test 4: compute_regional_disparity() includes rows for cuds and all 6 LCoGS indicators (7 variables x 5 regions = 35 rows minimum)
    - Test 5: _build_spatial_weights() returns a libpysal.weights.W object from a GeoDataFrame
  </behavior>
  <acceptance_criteria>
    - pyproject.toml has libpysal>=4.12, esda>=2.6, geobr>=1.0 added to dependencies
    - analysis/spatial_analysis.py exists with compute_morans_i(), compute_regional_disparity(), _build_spatial_weights(), _load_residuals() functions
    - compute_morans_i() loads FE residuals from run_panel_regressions(), builds queen contiguity spatial weights from geobr municipality shapefile, computes Moran's I via esda.Moran for each of 6 LCoGS outcomes, saves results/spatial/morans_i.csv
    - compute_regional_disparity() joins dimension_scores + lcogs_indicators + municipality_lookup on cod_ibge, groups by region, computes mean/median/q25/q75/IQR for CUDS and all 6 LCoGS indicators across all 5 macro-regions, saves results/tables/regional_disparity.csv
    - All tests pass with `uv run pytest tests/test_spatial_analysis.py -x`
  </acceptance_criteria>
  <action>
    **Step 1: Add dependencies to pyproject.toml**
    Add to the dependencies list:
    - `"libpysal>=4.12"` -- spatial weights construction (queen contiguity)
    - `"esda>=2.6"` -- exploratory spatial data analysis (Moran's I)
    - `"geobr>=1.0"` -- Brazilian municipality shapefiles (already used in compute_lcogs.py)
    - `"mapclassify>=2.8"` -- required by geopandas choropleth classification

    Run `uv lock` after adding (do NOT run uv sync yet).

    **Step 2: Create tests/test_spatial_analysis.py**
    Follow the test pattern from tests/test_ml_models.py:
    - Fixture `spatial_db(tmp_path)`: create minimal SQLite with dimension_scores (4 municipalities x 3 years), lcogs_indicators, and municipality_lookup tables. Use known region values ("Norte", "Sudeste") for 2 regions with 2 municipalities each.
    - Test `test_compute_morans_i_structure`: Mock geobr and esda to verify output DataFrame columns and interpretation logic. Use a mock GeoDataFrame with 4 point geometries and mock Moran result (I=0.3, p=0.01).
    - Test `test_morans_i_interpretation_positive`: With I > 0 and p < 0.05, interpretation should be "positive spatial autocorrelation".
    - Test `test_morans_i_interpretation_nonsignificant`: With I = -0.01 and p = 0.8, interpretation should be "no significant autocorrelation".
    - Test `test_regional_disparity_structure`: Call compute_regional_disparity() with the fixture DB, verify output has all required columns and all 5 regions appear (for the test DB, 2 regions). Verify 7 variables x 2 regions = 14 rows.
    - Test `test_regional_disparity_values`: With known input data, verify mean/median/IQR are mathematically correct.

    **Step 3: Create analysis/spatial_analysis.py**
    Module structure following the established pattern (run_regressions.py, ml_models.py):

    ```
    Module docstring with Outputs, Usage, Exports sections.
    Standard imports: argparse, logging, sqlite3, sys, pathlib.
    Lazy imports: geopandas, libpysal, esda, matplotlib (inside functions).
    Logger setup with %-style formatting.
    ```

    Constants:
    ```python
    LCOGS_OUTCOMES = [...]  # Same list from run_regressions.py (import it)
    REGION_ORDER = ["Norte", "Nordeste", "Centro-Oeste", "Sudeste", "Sul"]
    DISPARITY_VARS = ["cuds", "lcogs1_distance_km", "sao_per_100k",
                      "surgical_volume_per_100k", "pomr",
                      "financial_risk_ratio", "catastrophic_expenditure"]
    ```

    Functions:

    `_load_shapefile() -> gpd.GeoDataFrame`:
    - Use `geobr.read_municipality(year=2022)` (same pattern as compute_lcogs.py _load_municipality_centroids)
    - Normalize cod_ibge to 7-char string: `gdf["code_muni"].astype(str).str[:7]`
    - Module-level cache to avoid re-downloading

    `_build_spatial_weights(gdf: gpd.GeoDataFrame) -> libpysal.weights.W`:
    - Use `libpysal.weights.Queen.from_dataframe(gdf)` for queen contiguity
    - Log number of observations and mean neighbors

    `_load_residuals(db_path: Path) -> dict[str, pd.Series]`:
    - Import load_regression_panel, run_panel_regressions from analysis.run_regressions
    - Run regressions to get result objects
    - Extract residuals via `res.resids` for each outcome
    - Return dict mapping outcome_col -> residuals Series (indexed by cod_ibge, year)

    `compute_morans_i(db_path: Path, out_dir: Path, year: int = 2023) -> pd.DataFrame`:
    - Load residuals via _load_residuals()
    - Load shapefile via _load_shapefile()
    - For a single year (default 2023, most recent), extract per-municipality residuals
    - Merge residuals onto shapefile by cod_ibge
    - Build spatial weights via _build_spatial_weights()
    - For each LCoGS outcome: compute esda.Moran(residuals, weights)
    - Interpret: I > 0 and p < 0.05 -> "positive spatial autocorrelation"; I < 0 and p < 0.05 -> "negative spatial autocorrelation"; else -> "no significant autocorrelation"
    - Save to results/spatial/morans_i.csv with columns: [outcome, morans_i, p_value, z_score, n_obs, interpretation]
    - Add a comment: "Moran's I tests whether FE regression residuals exhibit spatial clustering. Significant positive autocorrelation suggests unmodeled spatial dependence."

    `compute_regional_disparity(db_path: Path, out_dir: Path) -> pd.DataFrame`:
    - Load dimension_scores + lcogs_indicators + municipality_lookup from SQLite
    - Join on cod_ibge to get region column
    - For each variable in DISPARITY_VARS, for each region in REGION_ORDER:
      - Compute: mean, median, q25 (25th percentile), q75 (75th percentile), iqr (q75-q25), n (count)
    - Save to results/tables/regional_disparity.csv
    - Log summary: "Regional disparity computed for {N} variables across 5 macro-regions"

    `main(argv) -> int`:
    - argparse: --db-path (default database/icskg_br.sqlite), --out-dir (default results), --year (default 2023)
    - Call compute_morans_i() and compute_regional_disparity()
    - Stage banners matching codebase pattern: `logger.info("=" * 60)` etc.

    Follow all codebase conventions:
    - %-style logging (never f-strings in logger calls)
    - Private functions prefixed with underscore
    - `if __name__ == "__main__": sys.exit(main())`
    - Section banners with `# ---------------------------------------------------------------------------`
    - Type hints on all function signatures
    - Docstrings with Parameters/Returns sections

    **Step 4: Run tests**
    ```
    uv run pytest tests/test_spatial_analysis.py -x -v
    ```
    Fix any failures. Ensure all 5+ tests pass.
  </action>
  <verify>
    <automated>cd "/Users/matheusrech/untitled folder 6/ICSKG" && uv run pytest tests/test_spatial_analysis.py -x -v</automated>
  </verify>
  <done>
    - pyproject.toml has libpysal, esda, geobr, mapclassify dependencies
    - analysis/spatial_analysis.py exists with compute_morans_i() and compute_regional_disparity()
    - Moran's I tests spatial autocorrelation of FE residuals for all 6 LCoGS outcomes
    - Regional disparity computes mean/median/IQR for CUDS + 6 LCoGS across 5 macro-regions
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publication-ready figures -- CUDS choropleth + LCoGS heatmaps</name>
  <files>analysis/spatial_analysis.py, tests/test_spatial_analysis.py</files>
  <read_first>
    @extract_lcogs1.py (existing choropleth pattern with geopandas + matplotlib)
    @analysis/spatial_analysis.py (Task 1 output -- the module to extend)
    @tests/test_spatial_analysis.py (Task 1 tests -- extend with visualization tests)
  </read_first>
  <behavior>
    - Test 1: generate_cuds_choropleth() creates a PNG file at the expected path
    - Test 2: The generated choropleth PNG has file size > 0 and is at least 300 DPI (check via PIL/matplotlib metadata)
    - Test 3: generate_lcogs_heatmaps() creates 6 PNG files (one per LCoGS indicator) in the figures directory
    - Test 4: Heatmap data has 5 regions (rows) x 9 years (columns) structure
  </behavior>
  <acceptance_criteria>
    - generate_cuds_choropleth() renders all municipalities with a colorblind-safe diverging palette (RdYlGn or coolwarm), state borders visible, legend, saved at 300 DPI to results/figures/cuds_choropleth.png
    - generate_lcogs_heatmaps() produces heatmaps for each LCoGS indicator showing temporal trends (2015-2023) across 5 macro-regions, saved at 300 DPI to results/figures/lcogs_heatmap_{indicator}.png
    - All figures use SciencePlots style context for publication quality
    - All new tests pass alongside Task 1 tests
  </acceptance_criteria>
  <action>
    **Step 1: Add visualization tests to tests/test_spatial_analysis.py**
    - Test `test_generate_cuds_choropleth_creates_file`: Mock geobr shapefile with a small GeoDataFrame (4 municipalities with simple square geometries). Call generate_cuds_choropleth() with the test DB. Assert output PNG exists and has size > 0.
    - Test `test_choropleth_dpi`: Read the saved PNG with PIL.Image, check metadata or verify file size is consistent with 300 DPI output (>100KB for even a simple map).
    - Test `test_generate_lcogs_heatmaps_creates_files`: Call generate_lcogs_heatmaps() with the test DB. Assert 6 PNG files exist in figures/ directory matching pattern `lcogs_heatmap_*.png`.
    - Test `test_heatmap_data_structure`: Call `_build_heatmap_data()` (internal helper) with test DB to verify it returns a pivot table with 5 region rows and year columns.

    **Step 2: Add visualization functions to analysis/spatial_analysis.py**

    `_build_heatmap_data(db_path: Path, indicator: str) -> pd.DataFrame`:
    - Load lcogs_indicators + municipality_lookup from SQLite
    - Join on cod_ibge, group by (region, year), compute median of indicator
    - Pivot: rows = regions (ordered by REGION_ORDER), columns = years (2015-2023)
    - Return pivot table

    `generate_cuds_choropleth(db_path: Path, out_dir: Path, year: int = 2023) -> Path`:
    - Lazy import: geopandas, matplotlib (with `matplotlib.use("Agg")`), SciencePlots
    - Use `plt.style.context(["science", "no-latex"])` for publication quality (SciencePlots). If SciencePlots not available, fall back to default style with publication-quality rcParams.
    - Load shapefile via _load_shapefile()
    - Load dimension_scores from SQLite for the specified year
    - Merge cuds onto shapefile by cod_ibge
    - Create figure: `fig, ax = plt.subplots(figsize=(16, 14))`
    - Plot with colorblind-safe diverging palette: use `cmap="RdYlGn"` (red=low CUDS, green=high). Alternatives if colorblind concern: `"coolwarm"` or `"cividis"`.
    - Add state borders: load `geobr.read_state(year=2022)` and overlay with `edgecolor="black", linewidth=0.5, facecolor="none"`
    - Legend with label "CUDS Score" and horizontal orientation
    - Missing data in lightgrey
    - Title: f"Composite Urban Development Score (CUDS) — {year}"
    - Save at 300 DPI: `fig.savefig(out_path, dpi=300, bbox_inches="tight", facecolor="white")`
    - Return path to saved figure
    - Log: "CUDS choropleth saved to %s (%.1f MB)"

    `generate_lcogs_heatmaps(db_path: Path, out_dir: Path) -> list[Path]`:
    - For each (outcome_col, outcome_label) in LCOGS_OUTCOMES:
      - Build heatmap data via _build_heatmap_data()
      - Create figure with `plt.style.context(["science", "no-latex"])`
      - Use seaborn.heatmap (lazy import) with:
        - `annot=True, fmt=".1f"` for value labels
        - `cmap="YlOrRd"` for LCoGS indicators (lower=better for most)
        - `linewidths=0.5, linecolor="white"`
        - y-axis: 5 regions in REGION_ORDER
        - x-axis: years 2015-2023
      - Title: f"{outcome_label} — Regional Temporal Trends"
      - Save at 300 DPI
      - Append path to results list
    - Return list of saved paths

    Update `main()` to also call:
    - `generate_cuds_choropleth(db_path, out_dir, year=args.year)`
    - `generate_lcogs_heatmaps(db_path, out_dir)`
    With stage banners for each.

    **Step 3: Run all tests**
    ```
    uv run pytest tests/test_spatial_analysis.py -x -v
    ```
    Ensure all tests (Task 1 + Task 2) pass. Then run full test suite:
    ```
    uv run pytest --tb=short -q
    ```
    Verify zero regressions in existing tests.
  </action>
  <verify>
    <automated>cd "/Users/matheusrech/untitled folder 6/ICSKG" && uv run pytest tests/test_spatial_analysis.py -x -v && uv run pytest --tb=short -q</automated>
  </verify>
  <done>
    - results/figures/cuds_choropleth.png exists at 300 DPI with colorblind-safe diverging palette, state borders, legend
    - 6 LCoGS heatmap PNGs exist showing regional temporal trends (2015-2023)
    - SciencePlots styling applied for publication quality
    - All spatial analysis tests pass
    - Zero regressions in existing test suite
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_spatial_analysis.py -x -v` -- all spatial tests pass
2. `uv run pytest --tb=short -q` -- full suite passes with no regressions
3. `uv run python analysis/spatial_analysis.py --db-path database/icskg_br.sqlite --out-dir results` -- produces all output files when run against real database
4. Verify results/spatial/morans_i.csv has 6 rows (one per LCoGS outcome) with I, p-value, interpretation
5. Verify results/tables/regional_disparity.csv has 35 rows (7 variables x 5 regions)
6. Verify results/figures/cuds_choropleth.png exists at 300 DPI
7. Verify results/figures/lcogs_heatmap_*.png -- 6 files at 300 DPI
</verification>

<success_criteria>
- SPAT-01: Moran's I statistic and p-value on FE residuals computed for all 6 LCoGS outcomes, logged to results/spatial/morans_i.csv with interpretation comment
- SPAT-02: CUDS choropleth renders all 5,570+ municipalities at 300 DPI with colorblind-safe diverging palette, state borders visible, legend
- SPAT-03: Regional disparity analysis: mean, median, IQR for CUDS and 6 LCoGS indicators across 5 macro-regions in results/tables/regional_disparity.csv
- SPAT-04: LCoGS indicator heatmaps by region with temporal trends (2015-2023) saved as 300 DPI publication-ready figures
</success_criteria>

<output>
After completion, create `.planning/phases/09-spatial-analysis/09-01-SUMMARY.md`
</output>
