---
phase: 09.1-methodology-enhancements
plan: 01
subsystem: analysis
tags: [econml, causal-inference, dml, causal-forest, pca, scipy]

# Dependency graph
requires:
  - phase: 05-dimension-computation-cuds
    provides: CUDS composite scores and 8 CIMI dimension scores in SQLite
  - phase: 06-lcogs-indicators
    provides: 6 LCoGS outcome indicators in SQLite
provides:
  - EconML causal inference module (LinearDML + CausalForestDML) for CUDS->LCoGS treatment effects
  - CATE heterogeneity analysis by region and population size
  - PCA composite score (already implemented in Phase 5, verified here)
affects: [10-publication-package, 09.1-03]

# Tech tracking
tech-stack:
  added: [econml>=0.15, scipy>=1.13]
  patterns: [lazy-import for econml matching codebase convention, control variable construction with one-hot region dummies]

key-files:
  created: [analysis/causal_inference.py]
  modified: [pyproject.toml]

key-decisions:
  - "LassoCV for LinearDML nuisance models (model_y, model_t) with 5-fold CV"
  - "CausalForestDML with 500 trees, min_samples_leaf=10 for stable CATE"
  - "Controls: region one-hot, log(population), year demeaned -- confounders for ecological study"
  - "CATE groups: 5 macro-regions + 3 population terciles for heterogeneous effects"
  - "scipy>=1.13 added as missing dependency (used by compute_dimensions.py and run_regressions.py)"

patterns-established:
  - "Causal inference pattern: Y,T,X,W array preparation with _prepare_arrays() helper"
  - "Graceful degradation: try/except per outcome with error dict fallback"

requirements-completed: [METH-01, METH-04]

# Metrics
duration: 3min
completed: 2026-04-02
---

# Phase 9.1 Plan 01: EconML Causal Inference + PCA Composite Summary

**EconML causal inference engine (LinearDML + CausalForestDML) with CATE heterogeneity by region/population, plus verified PCA composite score**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-02T05:06:05Z
- **Completed:** 2026-04-02T05:09:20Z
- **Tasks:** 2 (1 implemented, 1 verified existing)
- **Files modified:** 2

## Accomplishments
- Created analysis/causal_inference.py with LinearDML parametric ATE and CausalForestDML non-parametric ATE for all 6 LCoGS outcomes
- CATE heterogeneity analysis by macro-region (5 groups) and population tercile (small/medium/large)
- Added econml>=0.15 and scipy>=1.13 to pyproject.toml dependencies
- Verified PCA composite score (METH-04) already fully implemented in compute_dimensions.py with loadings, variance explained, and CUDS correlation

## Task Commits

Each task was committed atomically:

1. **Task 1: EconML causal inference module + dependencies** - `25d2b6a` (feat)
2. **Task 2: PCA composite verification** - No commit needed (already implemented in Phase 5)

## Files Created/Modified
- `analysis/causal_inference.py` - EconML DML + Causal Forest causal inference engine with CATE analysis
- `pyproject.toml` - Added econml>=0.15 and scipy>=1.13 dependencies

## Decisions Made
- Used LassoCV (not plain Lasso) for nuisance models to auto-tune regularization
- CausalForestDML with 500 trees and min_samples_leaf=10 balances estimation quality with computational cost
- Control variables include region dummies, log-population, and demeaned year to address ecological confounding
- CATE analysis uses both region (geographic heterogeneity) and population tercile (urban/rural gradient)
- Lazy imports for econml match codebase convention (same pattern as pysus, geopandas, geobr)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added scipy>=1.13 to pyproject.toml**
- **Found during:** Task 1 (dependency review)
- **Issue:** scipy imported by compute_dimensions.py and run_regressions.py but not declared in pyproject.toml
- **Fix:** Added scipy>=1.13 to dependencies under "Statistical computing" section
- **Files modified:** pyproject.toml
- **Verification:** Confirmed scipy imports in existing analysis modules
- **Committed in:** 25d2b6a (part of Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Essential missing dependency added. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Known Stubs
None - all functions are fully implemented with proper error handling and logging.

## Next Phase Readiness
- Causal inference module ready for execution against assembled SQLite database
- econml package must be installed (`pip install econml>=0.15`) before running
- PCA composite already operational from Phase 5
- Ready for Plan 02 (age-standardized rates, health regions, USD conversion) and Plan 03 (CI/dispersion + network distances)

---
*Phase: 09.1-methodology-enhancements*
*Completed: 2026-04-02*
