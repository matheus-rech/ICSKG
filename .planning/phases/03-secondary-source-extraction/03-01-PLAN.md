---
phase: 03-secondary-source-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract_ibge_sidra.py
  - tests/test_extract_ibge_sidra.py
  - tests/fixtures/sidra_6579_sample.json
  - tests/fixtures/sidra_5938_sample.json
  - tests/fixtures/sidra_4709_sample.json
  - data_sources/processed/ibge_sidra/.gitkeep
autonomous: true
requirements: [DATA-03]
must_haves:
  truths:
    - "Population estimates exist for all 5,570 municipalities for 2015-2023 (table 6579 for 2015-2021, table 4709 for 2022-2023)"
    - "GDP absolute values exist for all municipalities for 2015-2023 from table 5938"
    - "GDP per capita is computed as gdp / populacao (not fetched as a SIDRA variable)"
    - "All cod_ibge values are 7-digit zero-padded strings via normalize_cod_ibge()"
    - "Output Parquet files pass validate_dataframe() quality gate"
  artifacts:
    - path: "scripts/extract_ibge_sidra.py"
      provides: "SIDRA population + GDP extraction with per-capita computation"
      exports: ["extract_population", "extract_gdp", "main"]
    - path: "tests/test_extract_ibge_sidra.py"
      provides: "Offline unit tests with fixture data"
      min_lines: 60
    - path: "data_sources/processed/ibge_sidra/population.parquet"
      provides: "Population panel (cod_ibge, year, populacao)"
    - path: "data_sources/processed/ibge_sidra/gdp.parquet"
      provides: "GDP panel (cod_ibge, year, gdp_abs, gdp_per_capita)"
  key_links:
    - from: "scripts/extract_ibge_sidra.py"
      to: "database/utils.py"
      via: "normalize_cod_ibge() before Parquet write"
      pattern: "normalize_cod_ibge"
    - from: "scripts/extract_ibge_sidra.py"
      to: "database/validation.py"
      via: "validate_dataframe() quality gate"
      pattern: "validate_dataframe"
    - from: "scripts/extract_ibge_sidra.py"
      to: "sidrapy"
      via: "sidrapy.get_table() for SIDRA API"
      pattern: "sidrapy\\.get_table"
---

<objective>
Extract IBGE SIDRA population estimates and GDP data for all 5,570 Brazilian municipalities (2015-2023) into standardized Parquet files with computed GDP per capita.

Purpose: Population feeds LCoGS denominators (per-100K rates) and GDP feeds the Economy dimension (D2). Both are foundational for the entire panel.
Output: `scripts/extract_ibge_sidra.py`, population.parquet, gdp.parquet, test suite with fixtures
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-secondary-source-extraction/03-RESEARCH.md

@database/utils.py
@database/validation.py
@database/pipeline.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From database/utils.py:
```python
def normalize_cod_ibge(series: pd.Series) -> pd.Series:
    """Normalize Series of IBGE codes to 7-digit zero-padded strings."""

def load_ibge_municipios(path: Path | None = None) -> pd.DataFrame:
    """Load IBGE 2023 canonical municipality reference (5,571 rows)."""

def rename_municipality_column(df: pd.DataFrame) -> pd.DataFrame:
    """Rename source municipality column to 'cod_ibge'."""
```

From database/validation.py:
```python
def validate_dataframe(
    df: pd.DataFrame,
    source_name: str,
    strict: bool = False,
    quarantine_dir: Path | None = None,
) -> tuple[pd.DataFrame, ValidationReport]:
    """Validate DataFrame: municipality codes, year scope, duplicates, schema."""
```

From sidrapy (verified API):
```python
# sidrapy.get_table() returns DataFrame with columns:
#   D1C = Municipality code (7-digit IBGE)
#   V   = Value
#   D3C = Year code
# First row is metadata header -- always skip with df.iloc[1:]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test fixtures and test suite for SIDRA extraction</name>
  <files>tests/test_extract_ibge_sidra.py, tests/fixtures/sidra_6579_sample.json, tests/fixtures/sidra_5938_sample.json, tests/fixtures/sidra_4709_sample.json</files>
  <read_first>
    - database/utils.py (normalize_cod_ibge signature and behavior)
    - database/validation.py (validate_dataframe signature)
    - .planning/phases/03-secondary-source-extraction/03-RESEARCH.md (SIDRA column mapping, pitfalls 1-3, 6-7)
    - tests/test_sih_extract.py (existing test patterns and fixture usage)
  </read_first>
  <behavior>
    - Test 1: fetch_population_year(2021) with mocked sidrapy returns DataFrame with columns [cod_ibge, year, populacao], all cod_ibge are 7-digit strings
    - Test 2: fetch_population_year(2022) uses table 4709 (not 6579), variable 93 (not 9324)
    - Test 3: fetch_gdp_year(2021) returns DataFrame with [cod_ibge, year, gdp_abs] where gdp_abs is numeric (R$ 1,000)
    - Test 4: compute_gdp_per_capita merges population and GDP, producing gdp_per_capita = gdp_abs * 1000 / populacao
    - Test 5: first row of sidrapy response (metadata header) is skipped
    - Test 6: NaN values in V column are coerced (not crash)
    - Test 7: 2022-2023 GDP values are flagged as estimated in metadata column
  </behavior>
  <action>
    Create three JSON fixture files simulating sidrapy.get_table() responses:
    - sidra_6579_sample.json: 5 municipality rows for table 6579 (population 2021), including the metadata header row that must be skipped. Use columns NC, NN, MC, MN, V, D1C, D1N, D2C, D2N, D3C, D3N.
    - sidra_5938_sample.json: 5 municipality rows for table 5938 (GDP), variable 37, with values in R$ 1,000. Include one row with V="..." (missing) and one with V="-" to test coercion.
    - sidra_4709_sample.json: 5 municipality rows for table 4709 (Census 2022 population), variable 93.

    Create tests/test_extract_ibge_sidra.py with:
    - Fixtures loading the JSON files as DataFrames (simulating sidrapy output)
    - Tests mocking sidrapy.get_table() via monkeypatch to return fixture data
    - Tests for population extraction (table switching at year boundary 2021/2022)
    - Tests for GDP extraction and per-capita computation
    - Tests for metadata header skipping (per Pitfall 6 from RESEARCH.md)
    - Tests for NaN/missing value handling ("..." and "-" in V column per Pitfall 1)
    - Test that normalize_cod_ibge() is applied to all output cod_ibge values
    - Test that 2022-2023 GDP rows include an estimated flag

    Follow existing test patterns from tests/test_sih_extract.py.
    Use monkeypatch for sidrapy mocking (no network calls in tests).
  </action>
  <verify>
    <automated>cd "/Users/matheusrech/untitled folder 6/ICSKG" && .venv/bin/python -m pytest tests/test_extract_ibge_sidra.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All tests FAIL (RED phase) because scripts/extract_ibge_sidra.py does not exist yet
    - Fixture JSON files exist with realistic SIDRA response structure
    - Tests cover population table switching, GDP per-capita computation, header skipping, NaN handling
  </acceptance_criteria>
  <done>Test file runs (with expected failures), fixtures load correctly, all 7 test behaviors defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SIDRA population + GDP extractor</name>
  <files>scripts/extract_ibge_sidra.py, data_sources/processed/ibge_sidra/.gitkeep</files>
  <read_first>
    - tests/test_extract_ibge_sidra.py (test expectations to satisfy)
    - .planning/phases/03-secondary-source-extraction/03-RESEARCH.md (code examples, SIDRA column mapping, pitfalls)
    - database/utils.py (normalize_cod_ibge, load_ibge_municipios)
    - database/validation.py (validate_dataframe usage)
    - scripts/sih_extract.py (module pattern: main(argv=None), argparse, logging)
  </read_first>
  <action>
    Create scripts/extract_ibge_sidra.py following the codebase module pattern (main(argv=None), argparse CLI, logging with %-style, lazy imports with # noqa: PLC0415, section banners with 65-dash separators).

    Implement these functions:

    1. `fetch_population_year(year: int) -> pd.DataFrame`:
       - If year <= 2021: use sidrapy.get_table(table_code="6579", variable="9324", territorial_level="6", ibge_territorial_code="all", period=str(year))
       - If year >= 2022: use sidrapy.get_table(table_code="4709", variable="93", territorial_level="6", ibge_territorial_code="all", period=str(year))
       - Skip first row (metadata header) with df.iloc[1:] per Pitfall 6
       - Rename D1C -> cod_ibge, V -> populacao
       - Apply normalize_cod_ibge() to cod_ibge
       - Coerce populacao to numeric with errors="coerce" (handles "..." and "-" per Pitfall 1)
       - Add year column as integer
       - Return df[["cod_ibge", "year", "populacao"]]

    2. `fetch_gdp_year(year: int) -> pd.DataFrame`:
       - Use sidrapy.get_table(table_code="5938", variable="37", territorial_level="6", ibge_territorial_code="all", period=str(year))
       - Skip first row, rename D1C -> cod_ibge, V -> gdp_abs
       - GDP values are in R$ 1,000 (absolute, NOT per capita) per Pitfall 2 from RESEARCH.md
       - Apply normalize_cod_ibge()
       - Coerce gdp_abs to numeric
       - Add year column and estimated flag (True if year >= 2022, per Pitfall 3)
       - Return df[["cod_ibge", "year", "gdp_abs", "gdp_estimated"]]

    3. `extract_population(years: list[int], output_dir: Path, skip_existing: bool = True) -> Path`:
       - Loop through years, call fetch_population_year() for each
       - Concatenate all years into single DataFrame
       - Run validate_dataframe(df, source_name="ibge_population", strict=False)
       - Write to output_dir / "population.parquet"
       - Log row counts per year
       - Return output path

    4. `extract_gdp(years: list[int], output_dir: Path, population_path: Path, skip_existing: bool = True) -> Path`:
       - Loop through years, call fetch_gdp_year() for each
       - Concatenate, then merge with population data on (cod_ibge, year) to compute gdp_per_capita = gdp_abs * 1000 / populacao (per Pitfall 2: table 5938 values are in R$ 1,000)
       - Run validate_dataframe()
       - Write to output_dir / "gdp.parquet"
       - Return output path

    5. `main(argv=None)` with argparse:
       - --years: list of years (default 2015-2023)
       - --output-dir: default data_sources/processed/ibge_sidra/
       - --skip-existing: bool flag
       - Calls extract_population() then extract_gdp()

    Add retry logic with backoff for SIDRA API calls (3 retries, exponential backoff starting at 5 seconds). Wrap sidrapy.get_table() calls in try-except with graceful degradation per codebase conventions.

    Create data_sources/processed/ibge_sidra/.gitkeep for directory structure.
  </action>
  <verify>
    <automated>cd "/Users/matheusrech/untitled folder 6/ICSKG" && .venv/bin/python -m pytest tests/test_extract_ibge_sidra.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All tests in test_extract_ibge_sidra.py pass (GREEN phase)
    - Script follows codebase conventions (PEP 8, %-style logging, lazy imports, section banners)
    - Population uses table 6579 for 2015-2021, table 4709 for 2022-2023
    - GDP per capita computed manually from absolute GDP / population
    - 2022-2023 GDP flagged as estimated
    - normalize_cod_ibge() applied to all municipality codes before Parquet write
    - validate_dataframe() runs as quality gate before write
  </acceptance_criteria>
  <done>scripts/extract_ibge_sidra.py exists, all tests pass, population and GDP extraction functions work with correct table routing</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_extract_ibge_sidra.py -x -v` -- all tests pass
- `python -c "from scripts.extract_ibge_sidra import extract_population, extract_gdp; print('imports ok')"` -- module importable
- Population fixture covers table switching at 2021/2022 boundary
- GDP fixture covers missing value coercion and per-capita computation
</verification>

<success_criteria>
- scripts/extract_ibge_sidra.py exists with extract_population(), extract_gdp(), and main()
- All unit tests pass with mocked SIDRA API calls
- Table routing: 6579 for 2015-2021, 4709 for 2022+
- GDP per capita = gdp_abs * 1000 / populacao (not a SIDRA variable)
- 2022-2023 GDP flagged as estimated
- All cod_ibge values normalized via database/utils.py
</success_criteria>

<output>
After completion, create `.planning/phases/03-secondary-source-extraction/03-01-SUMMARY.md`
</output>
